<%= simple_form_for([word, answer], remote: true) do |f| %>
  <%= f.input :user_definition, as: :text, :input_html => { :rows => 2 } %>
  <% f.input :word, value: word %>
  <%= f.submit "Soumettre ma définition", class: "form-control" %>
<% end %>
